import { describe, expect, test } from '@jest/globals'

// TODO @Joana: Delete when other tests exist
// For now, Jest needs at least one test file to run successfully!
describe('template module', () => {
    test('example test', () => {
        expect(true).toBe(true)
    })
})
